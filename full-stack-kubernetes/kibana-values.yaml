elasticsearchURL: http://elasticsearch-master.efk.svc.cluster.local:9200

elasticsearchUsername: "elastic"
elasticsearchPassword:  "E57BvLHTobLTdUJN" # replace with actual password or use a Secret

ingress:
  enabled: false

service:
  type: ClusterIP
  port: 5601

resources:
  requests:
    cpu: 100m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 1Gi

replicaCount: 1

# Optional: avoid memory crashes
extraEnvs:
  - name: NODE_OPTIONS
    value: "--max-old-space-size=1800"

# Optional: fix warnings about encryption key
xpack:
  encryptedSavedObjects:
    encryptionKey: "c9cbf98843c0541febe50109f8e154849607c78d3372471a967f5ccbb9181ed6"  # must be 32 characters
  reporting:
    encryptionKey: "261947241e40061778363a45d37d2f1bf72d581020fcc1ff327f876082a572ce"
